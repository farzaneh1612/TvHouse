import {
  pxSizeGen,
  responsiveSize,
  priceFormatter,
  shadowGen,
  getData,
  storeData,
} from './functions';
import {setShadow, setSize, scaledSize, isDarkColor} from './styleFuncs';
export {
  pxSizeGen,
  responsiveSize,
  priceFormatter,
  shadowGen,
  getData,
  storeData,
  setShadow,
  setSize,
  scaledSize,
  isDarkColor,
};
